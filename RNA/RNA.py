import random
class RNA(object):
    def __init__(self, entradas: int, salidas : int, capas_ocultas : int):      
        self.entradas = entradas
        self.salidas = salidas
        self.n_ca

    def printt(self):
        print("Numero de salidas: {} Numero de salidas: {}".format(1,1))        
    
    class Perceptron(object):
        def __init__(self, input : int):
            self.input = input
            self.W = [] 
        def __create_pesos(self):
            self.aux = []
            for i in range(self.input):
                self.aux.add(random.randrange(0, 1000, 1)/1000)
            return
        def __feedforward(self):
            pass

        def __feedback(self):
            pass
